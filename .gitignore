# Secrets & config
.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/

# Backend runtime cache
backend/.prediction_dataset_cache.json
backend/.batch_output_cache.csv
generic-explainability/backend/.prediction_dataset_cache.json
generic-explainability/backend/.batch_output_cache.csv

# Node / frontend
frontend/node_modules/
frontend/dist/

# Data — not committed by default (may contain customer/training data).
# Specific public/synthetic example files are allowed via nested .gitignore
# exceptions (see generic-explainability/.gitignore).
data/*

# Root-level docs/ — legacy IL-app-specific documents, kept locally only,
# not part of the generic package (generic-explainability/docs/ is unaffected).
/docs/

# Reference notebooks & scratch files
ref/

# macOS
.DS_Store
