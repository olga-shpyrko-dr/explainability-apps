# Installed by DataRobot BEFORE start-app.sh runs — keep this minimal so the
# container can start uvicorn and pass /health within the ~2 min readiness window.
# Heavy deps (pandas, datarobot SDK) are in requirements-runtime.txt and installed
# during the Docker image build via build-app.sh.
fastapi>=0.115
uvicorn[standard]>=0.30
datarobot-asgi-middleware>=0.2.0
python-dotenv
pydantic-settings>=2.0
aiofiles>=23.0
