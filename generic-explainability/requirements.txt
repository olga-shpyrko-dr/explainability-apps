# Runtime dependencies for the explainability Custom Application.
# Installed automatically by DataRobot during application setup.
# Keep this lean — heavy or native deps (pyodbc, etc.) will break deploy.
fastapi>=0.115
uvicorn[standard]>=0.30
datarobot>=3.5
pandas>=2.0
anthropic>=0.28
litellm>=1.40
python-dotenv
pydantic-settings>=2.0
email-validator>=2.0
numpy
aiofiles>=23.0
