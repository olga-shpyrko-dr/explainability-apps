# Runtime dependencies for the explainability Custom Application.
# Installed by DataRobot during application setup AND by build-app.sh in the image.
# Keep lean — heavy optional deps live in requirements-llm.txt.
fastapi>=0.115
uvicorn[standard]>=0.30
datarobot>=3.5
datarobot-asgi-middleware>=0.2.0
pandas>=2.0
python-dotenv
pydantic-settings>=2.0
numpy
aiofiles>=23.0
